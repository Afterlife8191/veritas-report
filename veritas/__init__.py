"""Veritas: deterministic statistics, LLM prose, machine-checked claims.

The pipeline computes every metric and every comparison and decides nothing. A
language model reads those pre-computed numbers, decides what is worth saying,
and writes it. A deterministic validator then binds every number in the prose
back to the fact that produced it -- and the report does not ship unless every
one of them binds.
"""

__version__ = "1.0.0"
